import { FunctionComponent } from "react";
import {
  Table,
  TableBody,
  TableCell,
  TableContainer,
  TableHead,
  TableRow,
  Paper,
} from "@mui/material";
import { IGallery } from "../../types";

const DisplayData: FunctionComponent<IGallery[] | undefined> = (
  galleryList
) => {
  return <></>;
};

export default DisplayData;
